name: project_neo
description: "Project Neo - Red Social SaaS Híbrida"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
    
  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # Supabase
  supabase_flutter: ^2.8.0
  
  # Navigation
  go_router: ^14.6.0
  
  # Functional Programming
  dartz: ^0.10.1
  
  # Utils
  equatable: ^2.0.5
  shared_preferences: ^2.3.3
  internet_connection_checker: ^3.0.1
  
  # UI & Animations
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.1
  heroicons: ^0.10.0
  shimmer: ^3.0.0
  flutter_staggered_grid_view: ^0.7.0
  
  # Icons
  cupertino_icons: ^1.0.8
  
  # Observability
  sentry_flutter: ^9.9.1
  package_info_plus: ^8.1.2
  device_info_plus: ^11.2.0
  
  # Sign in
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.3
  crypto: ^3.0.5
  
  # CAPTCHA (optional - see CAPTCHA_SETUP.md for manual configuration)
  # hcaptcha_flutter: ^1.1.0
  
  # Utils
  intl: ^0.20.2
  flutter_slidable: ^4.0.3
  timeago: ^3.7.0
  image_picker: ^1.2.1
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.12

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
  
  # Note: Using google_fonts package for Inter font (downloaded at runtime)
  # No local font files needed
